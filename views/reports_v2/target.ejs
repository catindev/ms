<section>
    <h2>
        <a id="target" href="#target" aria-hidden="true"></a>
        Целевые и нецелевые клиенты
    </h2>
    <p><small>Клиенты с заполненными профилями и распределённые на целевых и не целевых</small></p>


    <ul>

        <% if ( target.length ) { %>
            <li>Целевых — <b><%= target.length %></b></li>
        <% } else { %>
            <li>Целевых нет</li>
        <% } %>

        <% if ( no_target.length ) { %>
            <li>Нецелевых — <b><%= no_target.length %></b></li>
            <ul>
                <% _.mapKeys(no_target_reasons, (value, reason) => { %>
                    <li><small><%= reason %> — <b><%= value %></b></small></li>
                <% }); %>
            </ul>


        <% } else { %>
            <li>Нецелевых нет <br><small>Возможно менеджеры заполняют профили неправильно</small></li>
        <% } %>

    </ul>
</section>