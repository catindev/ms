<%

 function canEdit() {
     return user.access === 'manager' && ( !contact.user ||
            contact.user._id.toString() === user._id.toString() );
 }


 function isCustomFields() {
     if (fields.length > 0) {
         const checked = fields.filter( field => contact[field.id] );
         return checked.length;
     }
     return 0;
 }
%>
<!doctype html>
<html>
<head>
    <%- include ../common/page-title.ejs %>
    <style>
        .title {
            padding: .5rem 0;
        }

        .contact-item {
            padding: .5rem 0;
            font-size: 1rem;
            font-weight: 300;
        }

        .item-label {
            font-weight: 500;
        }
    </style>
</head>
<body>
    <%- include ../common/main-menu.ejs %>

    <div style="width: 300px; margin: 0 auto;">
        <div style="text-align: center; padding-top: 25%;font-size: 22px;">
            Какой клиент?
        </div>
        <div style="padding: 2rem 0;">
            <a href="contacts/no-target/<%= cid %>" type="button" class="button default" style="float: left;">
                Нецелевой
            </a>
            <a href="contacts/edit/<%= cid %>" type="button" class="button primary" style="float: right;">
                Целевой
            </a>
        </div>
    </div>
</body>
<script src="assets/js/jq.js"></script>
<script src="assets/js/utils.js"></script>
<script src="assets/js/contact-update.js"></script>
</html>